<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.requests.sesathap">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SesathaP">
        <activity android:name=".manageListingActivity"></activity>
        <activity android:name=".subscriptionActivity" />
        <activity android:name=".sellerProfileActivity" />
        <activity android:name=".buyerProfileActivity" />
        <activity android:name=".addPaymentActivity" />
        <activity android:name=".paymentActivity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".userTypeSelActivity" />
        <activity android:name=".sellerRegisterActivity" />
        <activity android:name=".userTypeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>