<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.requests.sesathap" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SesathaP" >
        <activity
            android:name=".MainActivity2"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.SesathaP.NoActionBar" >
        </activity>
        <activity android:name=".Edit_Seller_Profile" />
        <activity android:name=".Seller_Store_View" />
        <activity android:name=".Seller_View" />
        <activity android:name=".seller_store_registation" />
        <activity android:name=".paymentActivity" />
        <activity android:name=".loginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".userTypeSelActivity" />
        <activity android:name=".sellerRegisterActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>